# database (Phase 8)

PostgreSQL + Prisma. Models: User, Conversation, Message, Agent, Tool,
UserSettings. The Prisma schema and client will live here. Until then,
conversation state is kept in memory on the client (see `hooks/`).
